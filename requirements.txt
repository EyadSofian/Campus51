# ============================================================
# Campus 51 — Murshid Agent | Dependencies (LangChain 1.0 stack)
# آخر إصدار مستقر من LangChain/LangGraph (Oct 2025 GA)
# ============================================================

# --- نواة LangChain 1.0 ---
langchain>=0.3.0
langgraph>=0.2.0
langchain-core>=0.3.0

# --- مزود الموديل: Gemini (نفس اللي في بوت Botpress بتاعك) ---
# لازم 4.x: بيستخدم google-genai SDK الجديد اللي بيدعم gemini-embedding-001
# و output_dimensionality. (الموديل القديم text-embedding-004 اتشال يناير 2026)
langchain-google-genai>=4.0.0,<5.0.0

# --- RAG: Pinecone (vector store سحابي للإنتاج) ---
langchain-pinecone>=0.2.0
pinecone>=5.0.0

# --- WhatsApp webhook server ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0          # عشان نبعت ردود لـ WhatsApp + نكلّم Campus51 endpoint
pydantic>=2.9.0

# --- أدوات مساعدة ---
python-dotenv>=1.0.0   # تحميل المتغيرات من .env
tenacity>=8.2.0        # retry logic على 429 (rate limit)
langchain-text-splitters>=0.3.0
